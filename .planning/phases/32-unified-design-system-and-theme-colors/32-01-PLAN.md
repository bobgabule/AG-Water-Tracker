---
phase: 32-unified-design-system-and-theme-colors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every hardcoded hex in the audit table has a corresponding semantic @theme token"
    - "Input utility classes produce identical styling to existing inline patterns"
    - "Existing Tailwind classes referencing --color-primary, --color-secondary, etc. still work unchanged"
  artifacts:
    - path: "src/index.css"
      provides: "All semantic @theme color tokens and input utility classes"
      contains: "--color-surface-header"
  key_links:
    - from: "src/index.css"
      to: "Tailwind utility classes"
      via: "@theme CSS variables"
      pattern: "--color-"
---

<objective>
Add all semantic color tokens to the Tailwind v4 @theme block and define input utility classes so that Plan 02 and Plan 03 can reference them.

Purpose: Establish the single source of truth for all colors before any component migration begins.
Output: Expanded src/index.css with ~25 new semantic tokens and 2 input utility classes.
</objective>

<execution_context>
@C:/Users/Bobits/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bobits/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add semantic @theme color tokens</name>
  <files>src/index.css</files>
  <action>
Expand the existing @theme block in src/index.css with all semantic color tokens mapped from the audit table. Keep the 6 existing tokens (primary, secondary, accent, status-ok, status-danger, status-warning) and add the following new tokens below them.

Token definitions (exact hex values from audit):

```
/* Surfaces */
--color-surface-header: #5f7248;
--color-surface-header-hover: #4e6339;
--color-surface-modal: #526640;
--color-surface-dark: #3a4a2a;
--color-surface-darkest: #2a3520;
--color-surface-page: #c5cdb4;
--color-surface-card: #dfe4d4;
--color-surface-card-hover: #d5dbc8;
--color-surface-input: #e8ebe0;

/* Buttons */
--color-btn-confirm: #bdefda;
--color-btn-confirm-text: #506741;
--color-btn-action: #4a7c59;
--color-btn-dark: #3d5030;
--color-btn-dark-active: #334428;

/* Controls */
--color-control-active: #89a267;
--color-control-active-alt: #8ca074;

/* Teal palette */
--color-teal: #5a9494;
--color-teal-light: #7fb3b3;
--color-teal-hover: #4a8484;
--color-teal-btn: #cdf2fb;
--color-teal-btn-text: #5e8f94;

/* Text */
--color-text-heading: #5f7248;

/* Accent/Misc */
--color-super-admin: #800000;
--color-accent-gps: #6190d1;
--color-text-coral: #e8937c;
```

Do NOT remove or rename any existing tokens. The 6 existing tokens remain unchanged.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit</automated>
    <manual>Check that src/index.css has all 25+ new tokens in @theme block</manual>
  </verify>
  <done>All semantic tokens from the color audit exist in @theme and the app compiles without errors</done>
</task>

<task type="auto">
  <name>Task 2: Add input utility classes</name>
  <files>src/index.css</files>
  <action>
Add two reusable input utility classes inside the existing `@layer utilities` block in src/index.css. These classes capture the two input patterns used across ~15 instances.

Light input (used on sage/light pages):
```css
.input-light {
  @apply w-full px-3 py-2.5 bg-gray-50 border border-gray-200 rounded-lg text-sm placeholder:text-gray-400 focus:outline-none focus:ring-2 focus:ring-primary;
}
```

Dark input (used on dark-bg sheets/modals):
```css
.input-dark {
  @apply w-full px-3 py-2.5 bg-white/10 border border-white/20 rounded-lg text-sm text-white placeholder:text-white/30 focus:outline-none focus:ring-2 focus:ring-white/40;
}
```

Place these after the existing `.animate-shimmer` definition inside `@layer utilities`. These classes are optional conveniences -- plans 02/03 MAY use them for new code but existing inline input styles will be migrated incrementally.
  </action>
  <verify>
    <automated>npx tsc -b --noEmit</automated>
    <manual>Verify src/index.css contains .input-light and .input-dark in @layer utilities</manual>
  </verify>
  <done>Both input utility classes exist and the app builds successfully</done>
</task>

</tasks>

<verification>
1. `npx tsc -b --noEmit` passes
2. src/index.css @theme block contains all tokens from the audit table
3. src/index.css @layer utilities contains .input-light and .input-dark
4. No existing behavior changes (tokens are additive)
</verification>

<success_criteria>
src/index.css is the single source of truth for all app colors, with every audited hex value represented as a semantic token. Input utility classes are available for use.
</success_criteria>

<output>
After completion, create `.planning/phases/32-unified-design-system-and-theme-colors/32-01-SUMMARY.md`
</output>
